% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/XL_to_i4.R
\name{proc_i4}
\alias{proc_i4}
\title{Process XL input file into an EcoTest data object of class ETData}
\usage{
proc_i4(
  xlfile,
  CVs = list(K = 0.05, maxa = 0.1, L50_Linf = 0.05, Linf = 0.025, Index = 0.15, Catch =
    0.1, ML = 0.1, MA = 0.1, MV = 0.05, FM = 0.01, L5_L50 = 0.1, LFS_L50 = 0.1, VML =
    0.01),
  nint = 40,
  nsim = 100,
  retros = 6,
  extrapolate = T,
  minspan = 10
)
}
\arguments{
\item{xlfile}{Character string. The location and name of the Excel input file.}

\item{CVs}{List. A named list of errors than can be applied to the various features of the Excel file.}

\item{nint}{Positive integer. The number of interpolations for all time series data. Leave this alone as 40 is the default of the training dataset.}

\item{nsim}{Positive integer. The number of draws of input features. If nsim = 1, features are subject to very small error (essentially deterministic at input values)}

\item{retros}{Integer. The number of retrospective peels to conduct. Strips away time series data by retros years to obtain status estimates over the most recent years.}

\item{extrapolate}{Boolean. Should missing data prior to the first year of data or after the last year of data be imputed with the first and last observation? Note that indicator 4 requires complete time series data so extrapolation may be necessary.}

\item{minspan}{Positive integer. The minimum number of years that time series need to span to be processed.}
}
\description{
Process XL input file into an EcoTest data object of class ETData
}
\author{
T. Carruthers
}
